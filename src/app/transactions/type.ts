export interface ITableDataType {
  transactionHash: string;
  status: string;
  method: string;
  timestamp: string;
  blockHeight: number;
  from: string;
  to: string;
  txnValue: number;
  txnFee: number;
}
